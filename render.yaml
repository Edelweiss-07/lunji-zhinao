# Render 自动部署配置文件
# 推到 GitHub 后 Render 会自动读取，无需手动填任何配置
services:
  - type: web
    name: marine-engine-brain
    runtime: docker
    region: sin # 新加坡节点，国内访问最快
    plan: free # 免费层
    dockerfilePath: ./Dockerfile
    startCommand: bash /app/start.sh
    autoDeploy: true # GitHub 推送后自动更新
    healthCheckPath: / # 健康检查路径
